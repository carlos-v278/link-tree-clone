{% extends 'base.html.twig' %}

{% block title %}Ressource{% endblock %}

{% block body %}
    <h1>Ressource</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ressource.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ressource.name }}</td>
            </tr>
            <tr>
                <th>Url_image</th>
                <td>{{ ressource.urlImage }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ ressource.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_test_index') }}">back to list</a>

    <a href="{{ path('app_test_edit', {'id': ressource.id}) }}">edit</a>

    {{ include('test/_delete_form.html.twig') }}
{% endblock %}
