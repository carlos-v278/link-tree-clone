{% extends 'base.html.twig' %}

{% block title %}Social{% endblock %}

{% block body %}
    <h1>Social</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ social.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ social.name }}</td>
            </tr>
            <tr>
                <th>Social_url</th>
                <td>{{ social.socialUrl }}</td>
            </tr>
            <tr>
                <th>Url_icone</th>
                <td>{{ social.urlIcone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_test_index') }}">back to list</a>

    <a href="{{ path('app_test_edit', {'id': social.id}) }}">edit</a>

    {{ include('test/_delete_form.html.twig') }}
{% endblock %}
